package services

func Scan(entranceType string) {
	// Scan QR code
	// Get QR code data
	// Check if user exists
	// Check if user is already checked in (based on entranceType)
	// Check if user is checked in to the same entrance
	// Create timelog
	// Return timelog
}
